#문자열 s와 자연수 n이 입력으로 주어집니다. 문자열 s를 좌측 / 가운데 / 우측 정렬한 길이 n인 문자열을 한 줄 씩 프린트해보세요.
s,num = input('').split(' ')
num = int(num)

#좌측정렬 print(s.ljust(num))
#가운데정렬 print(s.center(num))
#우측정렬 print(s.rjust(num))
